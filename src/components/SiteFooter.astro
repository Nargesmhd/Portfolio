---
import { routes, contact } from '../lib/routes';
---

<footer class="site-footer">
  <p>
    Toronto, ON · <a href={`mailto:${contact.email}`}>email</a> ·
    <a href={contact.linkedin.href}>linkedin</a> ·
    <a href={contact.github.href}>github</a>
  </p>

  <p>
    {/* Deliberately not in the primary nav - reached from here instead. The
        link now goes to a position piece rather than a statement, so the label
        says so; the build claim stays here, where it is still measured. */}
    <a href={routes.accessibility}>Accessibility and AI</a> · Built and tested
    to WCAG 2.2 AA, in both themes
  </p>
</footer>
